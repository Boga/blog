{% extends "base.jinja2" %}

{% block content %}
    <ul>
        {% for note in notes %}
        <li>
            <a href="{{ url_for('show_note', note_id=note.id) }}">{{ note
            .title }}</a>
        </li>
        {% endfor %}
    </ul>
{% endblock %}